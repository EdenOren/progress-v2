/**
 * Shared by signup and the profile screen. Two forms write the same column, so
 * the bounds live in one place rather than drifting apart in each component.
 */
export const DISPLAY_NAME_MIN_LENGTH: number = 2;
export const DISPLAY_NAME_MAX_LENGTH: number = 50;
